@using GestaoAvaliacao.WebProject.Facade
@using GestaoAvaliacao.Util;
@{
    var idAnalytics = string.Empty;
    if (ApplicationFacade.Parameters.Exists(p => p.Key == EnumParameterKey.ID_GOOGLE_ANALYTICS.GetDescription()))
    {
        idAnalytics = ApplicationFacade.Parameters.Where(p => p.Key == EnumParameterKey.ID_GOOGLE_ANALYTICS.GetDescription()).FirstOrDefault().Value;
    }
}

<script>
    (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
        (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
        m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
    })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');

    ga('create', '@idAnalytics', 'none');
    ga('send', 'pageview');
</script>


